# ADR-BE-023：暂不引入的组件

* 状态：Accepted
* 日期：2026-08-03

## 决策

首期不引入：Redis、Kafka、RabbitMQ、NATS、ES、ClickHouse、API Gateway、Service Mesh、K8s、独立鉴权/同步服务、GraphQL、通用事件溯源/CRDT。

重评条件见原 ADR 条文（高频广播、多消费组、全文检索、独立团队扩缩等）。

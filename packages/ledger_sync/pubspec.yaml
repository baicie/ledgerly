name: ledger_sync
description: Client sync engine (pending mutations, push/pull apply).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dev_dependencies:
  test: ^1.31.1
